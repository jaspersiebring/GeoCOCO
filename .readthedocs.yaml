version: 3

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.9"
  jobs:
    post_create_environment:
      - curl -LsSf https://astral.sh/uv/install.sh | sh
      
    post_install:
      - uv sync --all-extras --dev
      
    pre_build:
     - uv run sphinx-apidoc -o docs/ geococo/

sphinx:
  configuration: docs/conf.py